## Phase 8 — TxLINE Kit 0.1.0 release

Prepares the initial npm and crates.io releases, dual-license archive contents, public safety documentation, automated clean-room consumer smoke, and final release operations.

### Candidate proof

- 55 unit tests, TypeScript builds, Rust workspace tests/clippy, and production audit pass.
- npm tarball installs into an empty consumer and passes ESM, CommonJS, subpath, and CLI checks.
- Cargo packages and recompiles the crate archive with the lockfile.
- Archive scans exclude credentials, signer material, restricted recordings, host paths, and operational scripts.

### Finalization still required

Registry-origin installs, public repository visibility, v0.1.0 tag/release, final CI, and the completed Phase 8 proof report are hard merge gates.
